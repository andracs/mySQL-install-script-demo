// Fill in with your data, and rename to settings.js

const settings = {
    host: "your-db-adress",
    user: "your-db-username",
    password: "your-password",
};

const extra = {
    database: "your-db-name",
    table: "your-table-name"
}

module.exports = { settings, extra };